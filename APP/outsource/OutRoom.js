import React from 'react';
import { createAppContainer } from 'react-navigation';

import SmartHome from './navigation/SmartHome';
const OutRoom = createAppContainer(SmartHome);
export default OutRoom;